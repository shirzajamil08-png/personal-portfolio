# Only needed when the API is deployed on a different domain than the front end.
# In development, vite.config.js proxies /api to http://localhost:5000 instead.
VITE_API_URL=https://your-api.onrender.com
